# 企业微信回调配置指南

## 问题：回调地址请求不通过

### 常见原因和解决方案

#### 1. 配置检查清单

**在软件中检查：**
- [ ] CorpID（企业ID）是否正确
- [ ] AgentID（应用ID）是否正确
- [ ] Secret（应用Secret）是否正确
- [ ] Token（自定义，至少3个字符）
- [ ] EncodingAESKey（自定义，43个字符）
- [ ] ngrok Token 是否已配置

**配置示例：**
```
CorpID: ww1234567890abcdef
AgentID: 1000002
Secret: abc123...xyz
Token: mytoken123
EncodingAESKey: abcdefghijklmnopqrstuvwxyz1234567890ABCDEFG
ngrok Token: 2abc...xyz
```

#### 2. 启动服务步骤

1. **保存配置**
   - 填写完所有配置项
   - 点击"保存配置"按钮

2. **启动服务**
   - 点击"一键启动"按钮
   - 等待日志显示：
     ```
     ✅ pyngrok 安装完成（首次）
     🌐 ngrok 隧道建立成功：https://xxx.ngrok.io
     ✅ 本地服务已启动，端口 8899
     ```

3. **复制回调URL**
   - 从软件界面复制完整的回调URL
   - 格式：`https://xxx.ngrok.io`

#### 3. 企业微信后台配置

1. **进入应用配置**
   - 登录企业微信管理后台
   - 进入"应用管理" → 选择你的应用
   - 找到"接收消息"或"API接收"配置

2. **填写回调配置**
   ```
   URL: https://xxx.ngrok.io
   Token: mytoken123（与软件中一致）
   EncodingAESKey: abcdefghijklmnopqrstuvwxyz1234567890ABCDEFG（与软件中一致）
   ```

3. **保存配置**
   - 点击"保存"
   - 企业微信会发送GET请求验证
   - 如果验证通过，显示"保存成功"

#### 4. 验证失败排查

**A. Token不匹配**
- 症状：403错误
- 解决：确保软件和企业微信后台的Token完全一致（区分大小写）

**B. ngrok隧道未建立**
- 症状：无法访问URL
- 解决：
  1. 检查ngrok token是否正确
  2. 检查网络是否能访问ngrok.com
  3. 查看软件日志是否有错误

**C. 端口被占用**
- 症状：本地服务启动失败
- 解决：
  1. 关闭其他占用8899端口的程序
  2. 或修改代码中的WXAPP_SERVER_PORT

**D. 签名验证失败**
- 症状：企业微信显示"验证失败"
- 解决：
  1. 确保Token完全一致
  2. 重启软件服务
  3. 检查软件日志中的错误信息

#### 5. 测试回调

配置成功后测试：

1. **在企业微信应用中发送消息**
   ```
   帮助
   ```

2. **查看软件日志**
   - 应该看到：`📨 收到指令 [用户ID]：帮助`
   - 应该收到回复消息

3. **测试AI功能**
   ```
   AI 你好
   ```
   或直接发送：
   ```
   分析当前库存
   ```

#### 6. 常见错误代码

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| 403 | Token不匹配 | 检查Token配置 |
| 404 | URL不正确 | 检查回调URL |
| 500 | 服务器错误 | 查看软件日志 |
| 超时 | 网络问题 | 检查ngrok隧道 |

#### 7. 调试技巧

**查看详细日志：**
- 软件"自动化"标签页的日志窗口
- 查看每个请求的处理情况

**测试ngrok隧道：**
```bash
curl https://xxx.ngrok.io
```
应该返回403（因为没有正确的签名参数）

**检查端口监听：**
```bash
lsof -i :8899
```
应该看到Python进程在监听

#### 8. 重要提示

⚠️ **ngrok免费版限制**
- 每次重启会生成新的URL
- 需要重新配置企业微信回调地址
- 建议使用付费版获得固定域名

⚠️ **Token安全**
- 不要使用过于简单的Token
- 定期更换Token和AESKey
- 不要在公开场合泄露配置信息

⚠️ **网络要求**
- 确保能访问ngrok.com
- 企业微信服务器能访问ngrok域名
- 本地防火墙允许8899端口

## 成功标志

✅ 企业微信后台显示"保存成功"
✅ 软件日志显示"本地服务已启动"
✅ 发送消息能收到回复
✅ AI功能正常工作

## 需要帮助？

如果以上步骤都无法解决问题：
1. 查看软件完整日志
2. 检查企业微信后台的错误提示
3. 确认所有配置项都正确填写
4. 尝试重启软件和服务
